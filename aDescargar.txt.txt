NodeJS(instalamos una sola vez global a la pc)

npm i express -g

npm i express-generator -g
Para crear aplicaciones en Node:
express nombreProyecto

Modulos que vamos instalando:

npm i axios (permite hacer solicitudes http)
npm i sha1 (encriptacion de password)
npm i @hapi/joi (validaciones)
npm i mysql (conexiones con mysql)
npm i knex (querys simplificadas de sql SQL query builder)
npm i dotenv (variables de entorno)
npm i jsonwebtoken (creacion de tokens basados en JSON)
npm i uuid (creacion de ids unicos)
npm i nodamailer (envios de mails)